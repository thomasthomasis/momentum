# Core — all platforms
requests>=2.31,<3
python-dotenv>=1.0,<2
pystray>=0.19,<1
Pillow>=10.0
psutil>=5.9,<6

# macOS — active-app + idle-time detection (Quartz/AppKit)
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# Windows and Linux need no extra pip packages:
#   Windows uses ctypes (stdlib) against user32/kernel32.
#   Linux shells out to `xprintidle` and `xdotool` (X11 only — install via your
#   distro's package manager, e.g. `sudo apt install xprintidle xdotool`).
#   Detection degrades gracefully (no crash) if those tools aren't present.
